import React from 'react';
import { CreatePostForm } from '../../components/Forms/CreatePost';

export const HomePage = () => (
  <main>
    <section>
      <CreatePostForm />
    </section>
  </main>
);
