import styled from 'styled-components';
import { StyledPostCard } from '../../PostList/PostCard/StyledPostCard';

export const StyledUserPostCard = styled(StyledPostCard)`
  .button__size {
    width: 6rem;
    align-self: flex-end;
  }
`;
